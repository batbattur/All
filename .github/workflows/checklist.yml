name: QA 
on: pull_request

jobs:
  checklist_job:
    permissions: 
      pull-requests: write
      contents: read
    runs-on: ubuntu-latest
    name: Checklist
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Checklist  
        uses: batbattur/contextual-qa-checklist-action@b869ba5faf1a3377eb8b8141af599bb567057a7d
        with:
          gh-token: ${{ secrets.GITHUB_TOKEN }}
          input-file: 'ChecklistFolder/QAchecklist.yml'
          comment-header: "Hey, by the way, we noticed the following changes:"
