name: QA 
on: pull_request

jobs:
  checklist_job:
    permissions: 
      pull-requests: write
      contents: read
    runs-on: ubuntu-latest
    name: Checklist
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Checklist  
        uses: batbattur/contextual-qa-checklist-action@8638555cbd247c1e4f4202d8c41ff67ee5713258
        with:
          gh-token: ${{ secrets.GITHUB_TOKEN }}
          input-file: 'ChecklistFolder/QAchecklist.yml'
          comment-header: "Hey, by the way, we noticed the following changes:"
