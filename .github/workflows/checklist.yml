name: QA 
on: pull_request

jobs:
  checklist_job:
    permissions: 
      pull-requests: write
      contents: read
    runs-on: ubuntu-latest
    name: Checklist
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Checklist  
        uses: batbattur/contextual-qa-checklist-action@58eaaa35f5bafc4f13ed36a42a6f4959c22fdc88
        with:
          gh-token: ${{ secrets.GITHUB_TOKEN }}
          input-file: '.github/QAchecklist.yml'
          comment-header: "Hey, by the way, we noticed the following changes:"
